%span.bold
  = link_to presenter.merchant.name, merchant_path(presenter.merchant)
  - if presenter.merchant == current_merchant
    %span.parantheses= t(:you)
%span.colon
  - if presenter.sale_type?
    = t(:wishes_to_sell)
  - else
    = t(:wishes_to_purchase)